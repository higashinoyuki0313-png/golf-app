<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.golf.app.golfapp.mapper.ReservationMapper">

    <select id="findAll"
            resultType="com.golf.app.golfapp.model.Reservation">

        SELECT
        id,
        lesson_id,
        user_id,
        status
        FROM reservations

    </select>

    <select id="findById"
            resultType="com.golf.app.golfapp.model.Reservation">

        SELECT
        id,
        lesson_id,
        user_id,
        status
        FROM reservations
        WHERE id = #{id}
    </select>

    <select id="findByLessonId"
            resultType="com.golf.app.golfapp.model.Reservation">

        SELECT
        id,
        lesson_id,
        user_id,
        status
        FROM reservations
        WHERE lesson_id = #{lessonId}

    </select>

    <select id="findByUserId"
            resultType="com.golf.app.golfapp.model.Reservation">

        SELECT
        id,
        lesson_id AS lessonId,
        user_id AS userId,
        status
        FROM reservations
        WHERE user_id = #{userId}
        ORDER BY id DESC

    </select>


    <insert id="save">

        INSERT INTO reservations(
        lesson_id,
        user_id,
        status
        )
        VALUES(
        #{lessonId},
        #{userId},
        #{status}
        )

    </insert>

    <update id="updateStatus">

        UPDATE reservations
        SET status = #{status}
        WHERE id = #{id}

    </update>

    <select id="findByLessonIdAndUserId"
            resultType="com.golf.app.golfapp.model.Reservation">

        SELECT
        id,
        lesson_id,
        user_id,
        status
        FROM reservations
        WHERE lesson_id = #{lessonId}
        AND user_id = #{userId}
        LIMIT 1
    </select>

</mapper>